# Productivity Reminder — Chrome extension (Manifest V3)

A small productivity reminder extension — add tasks, get notifications and inline reminders, track streaks.

## Features
- Add timed reminders
- Desktop notifications + inline overlay
- Snooze & complete actions
- Streak tracking
- Local storage (chrome.storage.local)

## Install locally (for users)
1. Download the latest release zip from this repo (Releases tab).
2. Unzip the folder.
3. Open Chrome → `chrome://extensions/`.
4. Toggle **Developer mode** ON.
5. Click **Load unpacked** and select the extension folder.

> No Chrome Web Store fee required if you install locally.

## To contribute / run locally
1. Clone the repo:
   ```bash
   git clone https://github.com/YOUR_USER/productivity-reminder.git
   cd productivity-reminder

